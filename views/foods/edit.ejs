<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editing <%= food.name %></title>
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>
    <div class="edit-body">
        <h1>Edit <%= food.name %> at <%= food.recipe %></h1>
        <form
            action="/users/<%= user._id %>/foods/<%= food._id %>?_method=PUT"
            method="POST"
        >
            <label for="name">Name</label>
            <input
                type="text"
                name="name"
                id="name"
                value="<%= food.name %>"
            />

            <label for="recipe">Recipe</label>
            <input type="text" name="recipe" id="recipe" value="<%= food.recipe %>">

            <button type="submit">Update Post</button>
        </form>
    </div>
</body>
</html>