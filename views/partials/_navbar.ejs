<nav>
  <div class="navbar">
    <h1 id="nav-header">tellin'</h1>
    <div class="nav-header">
      <div class="nav-links">
        <a href="/users">Explore tellin'</a>
        <% if (user) { %>
          <a href="/users/<%= user._id %>/posts">My Blog</a>
          <a href="/auth/sign-out">Sign Out</a>
        <% } else { %>
          <a class="nav-sign-in" href="/auth/sign-in">Sign In</a>
          <a class="nav-sign-up" href="/auth/sign-up">Sign Up</a>
        <% } %>
    </div>
    </div>
  </div>
</nav>